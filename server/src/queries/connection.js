"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDb = getDb;
var node_postgres_1 = require("drizzle-orm/node-postgres");
var pg_1 = __importDefault(require("pg"));
var env_1 = require("../lib/env");
var schema = __importStar(require("@db/schema"));
var relations = __importStar(require("@db/relations"));
var fullSchema = __assign(__assign({}, schema), relations);
var instance;
function getDb() {
    if (!instance) {
        var isLocal = env_1.env.databaseUrl.includes("localhost") || env_1.env.databaseUrl.includes("127.0.0.1");
        var pool = new pg_1.default.Pool({
            connectionString: env_1.env.databaseUrl,
            connectionTimeoutMillis: 5000, // 5 seconds connection timeout
            query_timeout: 5000, // 5 seconds query timeout
            ssl: isLocal ? false : { rejectUnauthorized: false },
        });
        instance = (0, node_postgres_1.drizzle)(pool, { schema: fullSchema });
    }
    return instance;
}
